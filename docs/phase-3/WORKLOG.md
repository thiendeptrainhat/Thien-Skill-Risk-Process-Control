# Phase 3 — work log

- 2026-08-28: User authorized Phase 3 (fixtures, independent forward tests, review, release tooling/documentation, three ZIPs). No installation, Git mutation or publication is included.
- Preserved baseline commit `d65fad595e0265dbe665477b6a5747faa5811139`, historical 1.0.0 archives and Phase 1/2 records.
- Round 1 froze 49 canonical files after shortening shared discovery description to 174 characters, updating 1.1.0 license-application declarations and candidate metadata. Four model runs were dispatched against this snapshot: U01, U02, U04, U19.
- Before additional dispatches, a stale Phase 2 registry evidence sentence was found: it described release metadata as 1.0.0 and behavioral tests as not yet run. Replaced only that sentence with a durable scope limitation: registry metadata is not proof of installation/runtime availability, and verification is scoped to retained evidence. Business instructions and fixtures did not change.
- Round 2 retains the resulting full 49-file snapshot. Round 1 remains historical evidence; all four dispatched cases were freshly rerun against round 2. No pass is transferred between snapshots.
- U01 round 1 executor reported an initial filename listing beyond its fixture before reading the harness. It reported no reading of other fixture content or grading material. The incident is retained in its original trace and is not suppressed.
- U06 round 1 likewise disclosed an initial neighboring-filename listing before harness intake; no other fixture contents, rubric, reviews or history were reported as read. Its live-lookup review preserves this protocol limitation. Selected NIST PDF passages were actually read; the oversized OSCAL catalog remains unverified.
- U12 also disclosed a preparatory filename-only listing outside its fixture before harness intake. Its trace reports no reading of neighboring content/rubric/history; the review preserves this protocol limitation.
- Evidence tooling additionally validates hashes of supplementary JSON/packet/image artifacts. Four negative/positive verifier self-tests were added; all 61 tooling tests passed. These are not model acceptance cases.
- Capture metadata for unavailable model identity was normalized to JSON null plus explicit `not_available`; raw answers/traces are unchanged. Executor-maintained traces are not represented as complete exported host transcripts.
- Final outcomes, unresolved gaps and platform limitations belong in the Phase 3 report after review; this log is not an acceptance declaration.
- All 29 current model variants were executed and reviewed; four round-1 runs remain executed_unreviewed and are excluded from current claims. Live external lookup, chat/Word/PDF comparison and actual Document-Evidence handoff have their own retained artifacts. No OCR engine was added or run on the degraded-scan fixture; platform-native vision followed local parsing/rendering.
- Three 1.1.0 ZIPs were built locally and checked against canonical hashes and baseline history. P01/P02 static receipts retain the exact documentation snapshots read at execution, so later report wording does not rewrite their evidence. The final release-report review is a separate P03 receipt.
